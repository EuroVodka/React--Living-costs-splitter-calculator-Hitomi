import React from 'react'

function App( ) {
	const [FirstIncome, setFirstIncome] = useState( initialState )
	const [SecondIncome, setSecondIncome] = useState( initialState )
	const [TotalCost, setTotalCost] = useState( initialState )
	return (
		<div />
	)
}


export default App
